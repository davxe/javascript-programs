const p1={
    name:'rahul',
    city:'mumbai',
    gender:'male',
    details:function()
    {
        return `${this.name} lives in ${this.city} (${this.gender})`
    }
}
console.log(p1)
console.log(p1.details())
const p2={
    name:'rakesh',
    city:'chennai',
    gender:'male',
    details:function()
    {
        return `${this.name} lives in ${this.city} (${this.gender})`
    }
}
console.log(p2)
console.log(p2.details())
const p3={
    name:'roopa',
    city:'tumkur',
    gender:'female',
    details:function()
    {
        return `${this.name} lives in ${this.city} (${this.gender})`
    }
}
console.log(p3)
console.log(p3.details())
const p4={
    name:'ravi',
    city:'hyd',
    gender:'male',
    details:function()
    {
        return `${this.name} lives in ${this.city} (${this.gender})`
    }
}
console.log(p4)
console.log(p4.details())


// using
